# CI Gate 配置文件示例
# 通过 --gate-config 指定

gate:
  # 各严重级别最大允许数量，超过则 CI 失败（exit code 1）
  # 不指定则使用默认值：critical=0, major=0, minor=无限, info=无限
  max_critical: 0
  max_major: 0
  max_minor: 10
  max_info: 50
